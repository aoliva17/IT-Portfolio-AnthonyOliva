This screenshots folder contains:

DHCP network topology illustrating VLAN segmentation and the placement of routing and DHCP services

DHCP server configuration showing address pool setup and network scope definitions

End-device IP configuration verifying successful DHCP address assignment and default gateway resolution

IP helper-address configuration demonstrating DHCP relay across multiple VLANs

Physical routing representation linking the logical topology to real or simulated network hardware

